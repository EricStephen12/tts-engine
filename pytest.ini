[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: marks tests that exercise the FastAPI app end-to-end
    performance: marks latency/throughput benchmark tests
    requires_model: marks tests that need real Kokoro model weights on disk
